/** Shared staff / supervisor / reviewer signature pad (matches Chief sign-off). */
export const SIGNATURE_CANVAS_STYLE = {
  width: 'clamp(200px, 20vw, 400px)',
  aspectRatio: '1 / 1',
  height: 'auto',
  background: '#fff',
  borderRadius: 4,
}
